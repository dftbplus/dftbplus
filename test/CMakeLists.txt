add_subdirectory(prog/dftb+)
if (WITH_API)
  add_subdirectory(api/mm)
endif()
if (WITH_API AND BUILD_SHARED_LIBS)
  add_subdirectory(api/pyapi)
endif()
add_subdirectory(prog/modes)
if(WITH_TRANSPORT)
  add_subdirectory(prog/phonons)
endif()  
add_subdirectory(tools/dptools)
